# KripkeTactic

`KripkeTactic` is a compatibility wrapper over:

- `Logic.IPC.*` (syntax and Kripke semantics)
- `FastProofTheory.IPC.Unprovability` (`finish_unprovable`)

Runtime tooling for `rg3ied` lives in `tools/`.
